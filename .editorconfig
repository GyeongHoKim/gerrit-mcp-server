# Editor-agnostic formatting rules.
#
# The .vscode and .zed settings already say most of this, but only to their own
# editor. This file is what everyone else reads, and it is the layer that keeps
# a stray save from reformatting a file the tooling cares about.
#
# Formatting of Go code is gofumpt's job, not an editor's -- the [*.go] block
# below only describes what gofumpt already produces.

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.go]
indent_style = tab
indent_size = 4

[{go.mod,go.sum}]
indent_style = tab
indent_size = 4

# just recipes are indented with four spaces; a tab would work but mixing the
# two inside one recipe is an error.
[justfile]
indent_style = space
indent_size = 4

# Golden files are the expected output of internal/render, compared byte for
# byte. Two of them (diff_added, diff_modified) end lines in a space on
# purpose. Trimming it, or adding a newline render never emitted, breaks the
# test with a diff that looks identical on screen.
[*.golden]
trim_trailing_whitespace = false
insert_final_newline = false

# Two trailing spaces are a hard line break in Markdown.
[*.md]
trim_trailing_whitespace = false
